services:
  api:
    build:
      context: .
      target: runner
    env_file:
      - .env
    environment:
      NODE_ENV: production
      HOST: 0.0.0.0
      PORT: 5000
      GOOGLE_APPLICATION_CREDENTIALS: ""
    ports:
      - "5000:5000"
